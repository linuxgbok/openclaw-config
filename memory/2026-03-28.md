# 2026-03-28 记忆

## 今日事件
- ✅ 完成 OpenClaw 备份
- ✅ 配置飞书聊天（3 个账号）
- ✅ 创建 3 个 Agent（main, code-agent, exec-agent）
- ✅ 完成 Agent 测试
- ✅ 配置三层记忆系统

## 重要对话
- 用户要求配置飞书聊天
- 用户要求创建 code 和 exec 两个专用 Agent
- 用户要求配置三层记忆系统

## 决策记录
- 使用 `cli_a92943b24079dbd6` 作为 main 和 code 的飞书应用
- 使用 `cli_a94dcd9846781bd3` 作为 exec 的飞书应用
- code-agent 使用 `coding` 工具 profile
- exec-agent 使用 `full` 工具 profile
- 每个 Agent 都有独立的记忆目录

## 配置详情

### Agents
| Agent | 飞书应用 | 工具 Profile |
|-------|---------|-------------|
| main | main | default |
| code-agent | code | coding |
| exec-agent | exec | full |

### 记忆系统
- **短期记忆**：会话上下文（自动）
- **中期记忆**：每日记忆文件 `memory/YYYY-MM-DD.md`
- **长期记忆**：`MEMORY.md` + Agent 专属记忆

## 待跟进
- 观察记忆系统效果
- 根据使用情况调整记忆配置

## 备注
- 用户飞书 ID：ou_14845dcc102a1ba8b2a0af9cb1e29d2c (main/code), ou_840eef2cbe61f25239a0b03f94eb7d08 (exec)

## 教训记录
- **严重**: 创建飞书文档后未验证内容直接发送，导致用户收到空白文档
- **改进**: 所有文档必须先 `feishu_doc read` 验证内容，再发送链接
- **影响**: 已更新所有 Agent 的长期记忆，确保不再犯同样错误

## 新增规则
- **2026-03-28**: 所有 Agent 必须遵守 - 修改 openclaw.json 前必须执行 `~/.openclaw/scripts/backup-config.sh` 做完整备份

## 配置变更记录
- **15:26**: 合并 main 和 code-agent 为一个代码助手
- **15:31**: 配置 Token 节约策略
  - 记忆召回：main 5条, exec 3条
  - 关闭飞书打字指示器和用户名解析
  - 关闭流式输出，消息分块改为1000字
